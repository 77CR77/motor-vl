<?php
// Пример настроек. Скопируйте этот файл рядом под именем config.local.php
// и вставьте настоящие значения. config.local.php не попадает в git —
// именно поэтому секреты держим в нём, а не в коде сайта.
//
//     cp api/config.example.php api/config.local.php
//
// Ни один из этих файлов браузер отдать не может: PHP-файлы сервер исполняет,
// а не показывает, плюс в папке api лежит .htaccess, закрывающий config-файлы.

return [
    // Пароль к панели /admin.
    'admin_password' => 'придумайте-пароль',

    // Бот из @BotFather. Без этих двух значений сайт работает как обычно,
    // просто заявки не дублируются в чат.
    'telegram_token' => '',
    // id группы менеджеров, у групп он отрицательный: -1001234567890
    'telegram_chat_id' => '',
];
